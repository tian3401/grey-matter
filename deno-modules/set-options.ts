import {Options} from '../constants/interfaces.ts';

//TO DO: Finish function to set options for problem class
function setOptions(options: Options) {
  const {problem_type, parameters} = options;


}

export default setOptions; 